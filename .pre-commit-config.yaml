repos:
  - repo: local
    hooks:
      - id: ci-quality-gates
        name: ci-quality-gates
        entry: bash scripts/run-ci-quality-gates.sh --lane=fast-r1r2 --fix --stage
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
